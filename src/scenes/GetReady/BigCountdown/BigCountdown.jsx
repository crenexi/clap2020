import React from 'react';
import Countdown from '@components/shared/Countdown';
import BigCountdownView from './BigCountdownView';

const BigCountdown = () => (
  <Countdown component={BigCountdownView} />
);

export default BigCountdown;
