import React from 'react';
import { Countdown } from '@components/campaign';
import BigCountdown from './BigCountdown';

const BigCountdownContainer = () => (
  <Countdown component={BigCountdown} />
);

export default BigCountdownContainer;
